import time
import csv

import ReportUtil

currentTime= time.time()
# csvFile = open('names.csv', 'w+',newline='')
# fieldnames = ['event', 'time']
# writer = csv.DictWriter(csvFile,fieldnames)
# writer.writeheader(fieldnames)


# def toLine(string,currentTime):
#     return
#     writer.writerow(string, recordTime(currentTime))
# def recordTime(currentTime):
#     cost = time.time()-currentTime
#     ReportUtil.currentTime= time.time()
#     return cost
